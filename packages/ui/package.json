{
  "name": "@an/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "shadcn/ui components themed with the brand-* palette and Inter font. Web-only (Central Command). Field App and NyaliTrack use react-native primitives directly.",
  "scripts": {
    "lint": "echo 'placeholder'",
    "type-check": "tsc --noEmit",
    "test": "echo 'placeholder'"
  }
}
