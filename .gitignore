# Dependencies
node_modules
.pnpm-store

# Build output
.next
.turbo
dist
build
out
*.tsbuildinfo

# Environment — never commit (SRS §6.1)
.env
.env.local
.env.*.local
!.env.example

# Coverage
coverage
.nyc_output

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# Expo
.expo
.expo-shared
web-build

# Mobile native build artifacts
ios/Pods
ios/build
android/.gradle
android/build
android/app/build
android/local.properties

# Vercel
.vercel

# DB local volumes
infra/data

# Raw reference datasets — large, downloaded per dev machine.
# The derived ward-boundaries.ts (small) IS checked in.
tools/data/*.json
tools/data/*.geojson
tools/data/*.zip
tools/data/*.sql

# ngrok tunnel policy (contains basic-auth credentials)
ngrok-policy.yml

# --- Sensitive: credential dumps / PII (never commit) ---
Login-Credentials.pdf
*-LOGINS.pdf
*credentials*.pdf
fix-admin-login.ps1
packages/auth/src/dev/roster-data.json

# Contains hardcoded admin passwords — local only
packages/auth/src/dev/set-tiered-passwords.ts

# DB dumps — contain PII, never commit
*.dump
alfayo_dev_*.sql
